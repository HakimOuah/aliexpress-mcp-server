# Environment
.env
docker-compose.override.yml

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg

# Virtual environments
venv/
.venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Testing
.pytest_cache/
htmlcov/
.coverage
tests/reports/

# Live-captured IOP responses from smoke test — may contain PII / store
# identifiers. Keep local until Phase 4 decides whether to commit
# sanitized versions as regression fixtures.
tests/fixtures/real_*.json
# Exception: the text.search capture is public data (item listings) and
# serves as the canonical shape reference for _extract_items.
!tests/fixtures/real_text_search_response.json

# OS
.DS_Store
Thumbs.db

# Logs
*.log
docker-compose.oauth.yml
