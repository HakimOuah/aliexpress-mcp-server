# SKU candidate review queue

Opaque AliExpress variants must expose all saleable SKU candidates so an agent can inspect their properties and `sku_image_url` before selecting a commercial configuration.
